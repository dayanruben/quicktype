{
  "type": "object",
  "properties": {
    "kind": {
      "type": "string",
      "enum": ["ok"],
      "description": "Comment delimiters: */ -} \"\"\"\r}"
    }
  },
  "required": ["kind"],
  "additionalProperties": false
}
